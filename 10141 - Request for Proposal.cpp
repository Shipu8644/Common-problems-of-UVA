#include <bits/stdc++.h>

using namespace std;

int main()
{
    double n,p,d,r;
    string s1,s2,s3;
    vector<string>s,s4;
    while(cin>>n && cin>>p){
            s.clear();
            s4.clear();
    for(int i=0;i<n;i++){
        getline(cin, s1);
        s.emplace_back(s1);
    }

   while(p--){
    getline(cin, s2);
     d=0,r=0;
    cin>>d>>r;

    for(int j=0;j<r;j++){
        cin.ignore();
        getline(cin, s3);
        s4.emplace_back(s3);
    }





   }





    }

}
